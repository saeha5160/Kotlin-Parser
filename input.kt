package my.demo
import kotlin.text.*

fun main(args : Array<String>) {
   fun StringLen(obj:Any) : Int ? {
      if (obj is String && obj.length > 0 && (obj != null || 1 < 0))
         return obj.length
     
   }
   println(aras)
}


